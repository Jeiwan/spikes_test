h1 Товары

table.products.table
  thead
    tr
      th Наименование
      th Количество
      th Цена
      th Минимальный остаток
  tbody
    - @products.each do |product|
      tr
        td = product.article.name
        td = product.quantity
        td = product.price
        td
          = form_for product, url: admin_set_product_quantity_threshold_path(product.id), method: :patch, remote: true do |f|
            = f.text_field :quantity_threshold
            = f.submit "Задать"
